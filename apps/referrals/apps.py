from django.apps import AppConfig


class ReferralsConfig(AppConfig):
    """Config for the referrals app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.referrals"
    label = "referrals"
