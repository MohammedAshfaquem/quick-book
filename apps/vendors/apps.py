from django.apps import AppConfig


class VendorsConfig(AppConfig):
    """Config for the vendors app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.vendors"
    label = "vendors"
